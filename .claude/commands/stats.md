---
description: Show dictionary coverage and the review queue
---

Run `python3 scripts/validate.py` then `python3 scripts/stats.py` and show the
output.

Then add one short paragraph the script cannot: where the coverage is lopsided
and what would most improve the dictionary next. Domains with a handful of
entries and no `seen_at` provenance are usually the thin ones. Say which source
would be worth capturing to fill the gap.
